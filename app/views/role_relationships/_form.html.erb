<%= form_for(@role_relationship) do |f| %>
  <% if @role_relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_relationship.errors.count, "error") %> prohibited this role_relationship from being saved:</h2>

      <ul>
      <% @role_relationship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role_id %><br>
    <%= f.number_field :role_id %>
  </div>
  <div class="field">
    <%= f.label :revealed_role_id %><br>
    <%= f.number_field :revealed_role_id %>
  </div>
  <div class="field">
    <%= f.label :revealed_allegiance %><br>
    <%= f.check_box :revealed_allegiance %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
