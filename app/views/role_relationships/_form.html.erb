<%= form_for(@role_relationship) do |f| %>
  <% if @role_relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_relationship.errors.count, "error") %> prohibited this role_relationship from being saved:</h2>

      <ul>
      <% @role_relationship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role %><br>
    <%= f.collection_select(:role_id, Role.all, :id, :name, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :revealed_role %><br>
    <%= f.collection_select(:revealed_role_id, Role.all, :id, :name, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :revealed_faction %><br>
    <%= f.check_box :revealed_faction? %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
