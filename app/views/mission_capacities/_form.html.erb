<%= form_for(@mission_capacity) do |f| %>
  <% if @mission_capacity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mission_capacity.errors.count, "error") %> prohibited this mission_capacity from being saved:</h2>

      <ul>
      <% @mission_capacity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_count %><br>
    <%= f.number_field :player_count %>
  </div>
  <div class="field">
    <%= f.label :mission_number %><br>
    <%= f.number_field :mission_number %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="field">
    <%= f.label :fail_count %><br>
    <%= f.number_field :fail_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
